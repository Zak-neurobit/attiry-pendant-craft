server {
    listen 80;
    listen [::]:80;
    
    # Replace with your domain name
    server_name yoursite.com www.yoursite.com;
    
    # Path to your built React app (dist folder)
    root /var/www/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 10240;
    gzip_proxied expired no-cache no-store private must-revalidate pre-check=0 post-check=0 auth;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript;

    # Handle React Router - serve index.html for all routes
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Handle static assets with long cache
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|map)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header X-Content-Type-Options nosniff;
    }

    # Handle favicon specifically
    location = /favicon.ico {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Handle manifest.json
    location = /manifest.json {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Security headers
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy "strict-origin-when-cross-origin";

    # Hide nginx version
    server_tokens off;

    # Handle errors
    error_page 404 /index.html;
}

# HTTPS redirect (optional - if you have SSL)
# server {
#     listen 80;
#     listen [::]:80;
#     server_name yoursite.com www.yoursite.com;
#     return 301 https://$server_name$request_uri;
# }

# HTTPS configuration (optional - if you have SSL)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name yoursite.com www.yoursite.com;
#
#     # SSL configuration
#     ssl_certificate /path/to/your/cert.pem;
#     ssl_certificate_key /path/to/your/private.key;
#
#     # Include the above configuration here for HTTPS
# }